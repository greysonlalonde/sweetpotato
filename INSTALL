Thanks for downloading sweetpotato.

To install it, make sure you have Python 3.7 or greater (we recommend the latest) installed and add the following
dependencies:

    Node.js (https://nodejs.org/en/)
    Git (https://git-scm.com)
    yarn (https://classic.yarnpkg.com/en/docs/install)
    expo (https://docs.expo.dev/get-started/installation/)

and these two command from the command prompt:

    npm install --global yarn expo-cli
    python -m pip install .

For more detailed instructions, see docs/intro/install.txt.
